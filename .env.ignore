# .env.ignore - Items to skip when syncing from 1Password
#
# One item name per line. Supports exact matches and glob patterns.
# Use this for items that don't work as env vars (spaces, hyphens, multi-line values).

# Service account tokens (not valid env var names)
Service Account Auth Token: GitHub Actions - chatline

# Items with hyphens or multi-line values
chatline-changesets-bot
